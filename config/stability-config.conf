// Compose compiler + compose-stability-analyzer.
// One fully-qualified type per line. * = one package segment, ** = recursive.
// Use this for third-party / generated types that cannot take @Immutable.
// Our own UI models are annotated @Immutable in source.
kotlin.collections.List
kotlin.collections.Set
kotlin.collections.Map
